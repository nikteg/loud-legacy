/** @jsx React.DOM */

var React = require('react'),
    Image = require('./Image');

var Main = React.createClass({
    render: function() {
        return (
            <div></div>
        );
    }
});

module.exports = Main;
